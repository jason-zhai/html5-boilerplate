.ui.upward.dropdown[class*="pinned"] > .menu {
	bottom: 0;
	border-radius: .3rem;
}
.ui.dropdown[class*="top left pinned"] > .menu {
	left: -7px;
	top: -3px;
}
.ui.dropdown[class*="top left pinned"] > .glyphicons {
	z-index: 20;
}